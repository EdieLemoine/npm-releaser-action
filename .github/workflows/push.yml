name: 'On push to main'

on:
  push:
    branches:
      - on-push

jobs:
  release:
    uses: edielemoine/npm-releaser-action/.github/workflows/--release.yml@v1
    with:
      rebase: true
      pre_release_cmd: |
        echo "line 1"
        echo "line 2"
