name: 'Dispatch command'

on:
  issue_comment:
    types: [ created ]

jobs:
  dispatch:
    uses: edielemoine/npm-releaser-action/.github/workflows/--dispatch.yml@main
    secrets: inherit
    with:
      commands: |
        rebase
        python
